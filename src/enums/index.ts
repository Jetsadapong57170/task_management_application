export enum TaskStatus {
    ALL = "All",
    TO_DO = "To Do",
    IN_PROGRESS = "In Progress",
    DONE = "Done"
}

export enum TaskPriority {
    ALL = "All",
    LOW = "Low",
    MEDIUM = "Medium",
    HIGH = "High"
}

export enum TaskSort {
    ALL = "All",
    DUE_DATE = "Due date",
    PRIORITY = "Priority"
}

export enum TaskSortOrder {
    DESC_DUE_DATE = "Desc due date",
    ASC_DUE_DATE = "Asc due date",
    DESC_PRIORITY = "Desc priority",
    ASC_PRIORITY = "Asc priority",
}